# Degraded Mode

Implements: R-6.10.7 [verified], R-6.10.9 [verified]

## Overview

The system degrades by isolating the smallest safe scope: command, drive,
observer, item, or block scope. It does not silently keep going when the
current source of truth is missing or untrusted.

Stable degraded-mode IDs make the matrix auditable. Each row below maps to at
least one exact test name so the documentation can point at executable
behavior instead of broad package-level claims.

## Verified By

The failure matrix below is itself the evidence index for degraded behavior.
Each row includes exact executable evidence.

## Failure Matrix

| ID | Failure Source | Detection Owner | One-Shot Behavior | Watch Behavior | Persistence | Logging | User Recovery | Evidence |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `DM-1` | Config load or validation failure | `config` / CLI bootstrap | Abort before any data-plane work starts. | Initial startup aborts. Reload failure logs a warning and keeps the last good in-memory config. | None beyond the existing config file. | Fatal load errors at command start; reload failures at WARN. | Fix the config file or bad override and rerun/reload. | `internal/config/load_test.go` (`TestLoadLenient_TOMLSyntaxError_Fatal`, `TestLoadLenient_UnknownKeys_ReturnsWarnings`), `internal/config/failure_class_test.go` (`TestClassifyLoadOutcome`), `internal/multisync/orchestrator_test.go` (`TestOrchestrator_Reload_InvalidConfig`) |
| `DM-2` | Token load or refresh failure | `graph` auth boundary | Abort the current command as fatal auth failure. | The affected drive stops making progress until auth is repaired; other drives continue. | Token file remains authoritative; no retry row is invented for bad credentials. | Auth failure at ERROR or user-facing command error. | Re-login or repair the token/account. | `internal/graph/client_test.go` (`TestDo_TokenError`), `internal/graph/auth_test.go` (`TestTokenBridge_Error`), `internal/multisync/orchestrator_test.go` (`TestRunOnce_TokenError_ReportsPerDrive`) |
| `DM-3` | Graph transient/service failure (`408`, `423`, `429`, `5xx`, documented transient quirks) | `graph` + `sync` classifier | The pass records delayed work and returns; the next run replans from durable state. | Items become `retry_work` rows or activate `block_scopes`; unaffected work keeps running. | `retry_work` and, when needed, `block_scopes`. | Retry detail at DEBUG, scope/service degradation at WARN/INFO. | Wait for recovery or clear the underlying service issue. | `internal/sync/engine_result_classify_test.go` (`TestClassifyResult_HTTPPersistenceAndScopeRouting`), `internal/sync/engine_retry_trial_test.go` (`TestRunRetrierSweep_ReleasesHeldRetryEntriesOnly`, `TestRunRetrierSweep_DoesNotConsultDurableRetryRowsWithoutHeldRuntimeEntry`, `TestRunTrialDispatch_ReleasesFirstHeldScopeCandidateAsTrial`), `internal/sync/engine_results_test.go` (`TestWatchRuntime_ArmRetryTimer_KicksImmediatelyWhenRetryIsDue`) |
| `DM-4` | Store open/corruption/unsupported persisted state | `sync` store boundary / engine startup | Skip the affected drive, continue healthy drives, and exit non-zero after printing exact reset guidance. | Skip the affected drive on startup or reload while healthy drives continue. | The existing DB file family is preserved until the operator runs the explicit reset command. | Startup classification logs the failure; CLI/watch surfaces include the exact reset command to run. | Run `onedrive-go drive reset-sync-state --drive <drive>`, confirm the reset, then start sync again. | `internal/cli/status_test.go` (`TestStatusCommand_UnreadableStateStoreFallsBackToEmptySyncState`), `internal/cli/sync_runtime_test.go` (`TestRunSyncDaemonWithFactory_FormatsResetGuidanceWhenNoDriveStarts`), `internal/multisync/orchestrator_test.go` (`TestOrchestrator_RunWatch_SkipsIncompatibleStoreDriveWhenAnotherDriveStarts`, `TestOrchestrator_RunWatch_ReturnsStartupFailureWhenNoDriveStarts`), `internal/sync/engine_run_once_test.go` (`TestNewEngine_RequiresResetForNonSQLiteStateDB`, `TestNewEngine_RequiresResetForIncompatibleSchemaStateDB`, `TestNewEngine_RequiresResetForUnsupportedStoreGeneration`) |
| `DM-5` | Local permission denied | local observation plus `sync` permission probing | Observation records file-level read denial as durable blocked truth and keeps unrelated work running. | Observation records read-denied subtree boundaries on `observation_issues`; probe-driven timed write scopes keep readable work flowing while blocked writes are retried on the normal scope cadence. | `observation_issues` for read problems, `block_scopes` plus blocked `retry_work` for shared write blockers. | WARN summaries plus reason/action text in `status`. | Fix permissions or wait for automatic observation/trial recovery after access is restored. | `internal/sync/permission_handler_test.go` (`TestPermHandler_HandleLocalPermission_SyncRootInaccessible`, `TestPermHandler_HandleLocalPermission_DirectoryLevel`, `TestPermHandler_HandleLocalPermission_FileLevel`), `internal/sync/planner_sqlite_test.go` (`TestPlannerPlanCurrentState_LocalReadDeniedDoesNotDeleteRemoteData`, `TestPlannerPlanCurrentState_LocalReadBoundaryBlocksRemoteDeletesForDescendants`) |
| `DM-6` | Disk full / below `min_free_space` | `driveops` + `sync` classifier | Fail the affected download or abort further downloads in that pass as classified by disk policy. | Activate `disk:local` for downloads while allowing non-download work to continue. | `retry_work` for exact work and `block_scopes` for shared low-disk timing. | WARN/INFO for degraded scope; per-item detail at DEBUG. | Free disk space or lower/disable the reserve threshold intentionally. | `internal/sync/engine_scope_lifecycle_test.go` (`TestScopeController_ApplyTrialReclassification_RehomesDiskScopeRetryWork`), `internal/sync/store_scope_admin_test.go` (`TestPruneBlockScopesWithoutBlockedWork`) |
| `DM-7` | Local or remote observer failure | observer + engine | Abort the pass for that drive because fresh observation is unavailable. | Observer loops back off and retry; the watch engine stays alive and reuses the shared pipeline when the observer recovers. | No synthetic alternate source of truth; durable state stays in the store. | WARN/ERROR on observer failure with retry/backoff detail at DEBUG. | Wait for recovery or fix the local/network cause. | `internal/sync/engine_phase0_test.go` (`TestPhase0_RunWatch_BootstrapCompletesBeforeLocalObserverStarts`, `TestPhase0_RunWatch_BootstrapCompletesBeforeRemoteObserverStarts`), `internal/sync/engine_watch_test.go` (`TestRunWatch_AllObserversDead_ReturnsError`) |
| `DM-8` | Full remote refresh failure | watch-mode engine | Not applicable; full remote refresh is watch-only. | Log the failure and continue normal watch operation; the next full remote refresh interval retries. | Existing durable state remains authoritative; no partial refresh result is applied. | WARN/ERROR for the failed full remote refresh run. | Wait for the next cycle or inspect the logged error. | `internal/sync/engine_refresh_test.go` (`TestRunFullRemoteRefreshAsync_DeltaError`, `TestRunFullRemoteRefreshAsync_ShutdownAfterCommit`) |
| `DM-9` | Shutdown during in-flight work | CLI + engine lifecycle | Respect cancellation, stop admitting new work, drain what can finish, then exit. | First signal cancels the shared watch context, seals new admission, and lets already-admitted work settle if they finish before worker exit; second signal forces exit. | Partially completed work is recovered from store plus sync root on next start. | INFO for graceful shutdown, abrupt termination only when forced. | Restart the command; recovery and durable retry work resume unfinished work. | `internal/cli/signal_test.go` (`TestShutdownContext_FirstSignalCancels`, `TestRunSyncWatch_FirstSignalCancelsWatchRunner`, `TestRunSyncWatch_FirstSignalCancelsDaemonOrchestrator`, `TestShutdownContext_SecondSignalForcesExit`), `internal/sync/engine_watch_test.go` (`TestRunWatch_ContextCancel`, `TestRunWatch_CancellationWinsOverFinalObserverExit`, `TestRunWatch_ShutdownStopsRetryAndTrialTimers`, `TestRunWatch_ShutdownAfterCommittedRefreshDoesNotDropAppliedBatch`, `TestRunWatch_FallbackSleepHonorsCancellation`), `internal/sync/engine_refresh_test.go` (`TestRunFullRemoteRefreshAsync_ShutdownAfterCommit`), `internal/sync/fault_injection_test.go` (`TestFault_ContextCancel_WorkerPool`) |
| `DM-10` | Authenticated profile works but live drive catalog cannot be completed in the current command | CLI discovery surfaces | `status` keeps the authenticated account card visible and uses `/me/drive` fallback when possible. | Not applicable; this row is a CLI discovery boundary. | No new persisted discovery cache. | WARN on degraded discovery, plus structured JSON/text warning output. | Retry the discovery command later. | `internal/cli/auth_test.go` (`TestDiscoverLiveDriveCatalog_DegradesToPrimaryDrive`, `TestDiscoverLiveDriveCatalog_DegradesWithoutPrimaryDrive`), `internal/cli/drive_test.go` (`TestDiscoverAccessibleDrives_DegradesToPrimaryDrive`, `TestPrintDriveListText_IncludesDegradedSection`) |
