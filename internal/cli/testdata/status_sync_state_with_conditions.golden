    Conditions: 3
    Retrying:  2 items

    CONDITIONS
    QUOTA EXCEEDED (1 item)
      The OneDrive storage quota for this sync scope is full. Free up space in the owning drive, or ask the shared-folder owner to do so.
      Scope: Shared/Team Docs

      /quota/a.txt
      Next: Free up space in the owning drive, or ask the shared-folder owner to do so.

    INVALID FILENAME (2 items)
      The filename contains characters not allowed by OneDrive. Rename the file to remove invalid characters.

      /bad:name.txt
      /worse:name.txt
      Next: Rename the file to remove invalid characters.

    AUTHENTICATION REQUIRED (1 item)
      The last sync attempt for this account was rejected by OneDrive. Run 'onedrive-go status' to re-check access, or 'onedrive-go login' to sign in again.
      Scope: your OneDrive account authorization
      Next: Run 'onedrive-go status' to re-check access, or 'onedrive-go login' to sign in again.
